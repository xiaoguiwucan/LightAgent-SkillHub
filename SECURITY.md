# 安全政策

请勿在公开 Issue 中披露可利用漏洞、恶意技能细节或用户数据。通过 GitHub Security Advisory 私下报告，并附受影响技能、版本、复现步骤和影响。

确认恶意或失陷版本后，维护者将其标记为 `revoked`，阻止新安装并向已安装用户展示高优先级警告。Hub 不会远程删除用户本地文件。

版权、许可证或素材下架请求请创建“下架请求”Issue，并提供权利证明与准确路径。维护者可在调查期间先将版本标记为 `yanked`。
